import React from 'react';

const Sidebar = () => {
    return (
        <div className="hidden">
            {/* Sidebar component - currently integrated into Editor layout */}
        </div>
    );
};

export default Sidebar;
